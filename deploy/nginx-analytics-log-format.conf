# Include this file once inside the global Nginx `http` block.
# The dedicated format deliberately excludes IP addresses, user agents, referrers,
# cookies, request bodies, query strings, and any application identifier.
log_format profeplus_product_events '$time_iso8601 $uri';
